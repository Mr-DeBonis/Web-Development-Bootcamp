# Install npm module packages
NPM packages can be found at [npmjs.com](https://www.npmjs.com/).

The packages used for this project can be installed with npm:
```
npm install superheroes
npm install supervillains

```
